<h1>Roles</h1>

<p><%= link_to 'Add a New Role', new_role_url %></p>

<table>
  <thead>
    <tr>
      <th>Character name</th>
      <th>Movie</th>
      <th>Actor</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @roles.each do |role| %>
    <tr>
      <td><%= link_to role.character_name, role_url(role.id) %></td>
      <td><%= link_to role.movie.title, movie_url(role.movie.id) %></td>
      <td><%= link_to role.actor.name, actor_url(role.actor.id) %></td>

      <td><%= link_to 'Show', role_url(role) %></td>
      <td><%= link_to 'Edit', edit_role_url(role) %></td>
      <td><%= link_to 'Destroy', role_url(role), method: 'delete', data: { confirm: "Do you really want to delete this role?" } %></td>

    </tr>
  <% end %>
  </tbody>
</table>

